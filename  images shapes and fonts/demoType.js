function setup() {
	createCanvas(900, 600);
	textFont("Arial"); //set font
	textSize(22); //set font size
	
	
}
function draw() {
	text("hello world it's  a beautiful day outside'", 0,0, 200, 200) //print linked to mouse location
}